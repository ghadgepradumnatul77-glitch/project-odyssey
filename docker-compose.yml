services:
  db:
    image: postgres:16
    container_name: odyssey-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: odyssey
    ports:
      - "5432:5432"
    volumes:
      - odyssey_pgdata:/var/lib/postgresql/data

volumes:
  odyssey_pgdata:
