import{fireEvent,render,screen}from'@testing-library/react';import{afterEach,describe,expect,it,vi}from'vitest';import{AuthContext}from'../auth/AuthProvider';import PolicyActionsPage from'./PolicyActionsPage';const auth={token:'token',user:{role:'POLICY_ADMIN'},isAuthenticated:true}as any;const policy={id:'p',policyCode:'POLICY_PWD',versionNumber:2,title:'Bridge safety policy',sourceTitle:'Controlled synthetic policy source',sourceReference:'SYNTHETIC-REF',sourceUrl:null,effectiveFrom:'2026-08-20',effectiveUntil:null,status:'DRAFT',validationState:'NOT_VALIDATED',approvedAt:null,createdAt:'2026-08-20',updatedAt:'2026-08-20',department:null,jurisdiction:null,_count:{rules:0}};const action={id:'a',actionCode:'ACT_INSPECT_DETAILED',versionNumber:3,title:'Detailed assessment',category:'ASSESSMENT',description:'Governed action',applicability:{},enforcementClassification:'MANDATORY',sourceReference:'SYNTHETIC-REF',effectiveFrom:'2026-08-20',effectiveUntil:null,status:'ACTIVE',approvedAt:'2026-08-20',department:null,jurisdiction:null,provenancePolicyDocument:{id:'p',policyCode:'POLICY_PWD',versionNumber:2,title:'Policy'}};afterEach(()=>vi.unstubAllGlobals());
describe('Policy & Actions workspace',()=>{it('shows governed versions, provenance and enforcement without AI authority claims',async()=>{vi.stubGlobal('fetch',vi.fn(async(url:string)=>new Response(JSON.stringify({success:true,data:url.endsWith('/policies')?[policy]:[action]}),{status:200})));render(<AuthContext.Provider value={auth}><PolicyActionsPage/></AuthContext.Provider>);expect(await screen.findByText('Bridge safety policy')).toBeInTheDocument();expect(screen.getByText(/never becomes authoritative automatically/i)).toBeInTheDocument();fireEvent.click(screen.getByRole('button',{name:'Approved actions'}));expect(screen.getByText('Detailed assessment')).toBeInTheDocument();expect(screen.getByText(/ASSESSMENT · MANDATORY/)).toBeInTheDocument();expect(screen.getByText(/Policy POLICY_PWD v2/)).toBeInTheDocument();});});
