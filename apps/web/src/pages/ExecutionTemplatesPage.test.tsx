import{fireEvent,render,screen}from'@testing-library/react';import{afterEach,describe,expect,it,vi}from'vitest';import PolicyActionsPage from'./PolicyActionsPage';import{AuthContext,type AuthContextValue}from'../auth/AuthProvider';
const context:AuthContextValue={user:{id:'u',employeeCode:'E',name:'Admin',email:'a@test',designation:'Administrator',role:'POLICY_ADMIN',status:'ACTIVE',departmentId:'d',jurisdictionId:'j'},token:'token',organization:null,isAuthenticated:true,authStatus:'authenticated',sessionMessage:null,login:vi.fn(),logout:vi.fn()};const response=(data:unknown)=>new Response(JSON.stringify({success:true,data}),{status:200});afterEach(()=>vi.unstubAllGlobals());
describe('Execution Template registry workspace',()=>{it('renders exact action/template versions, draft state and human boundary',async()=>{vi.stubGlobal('fetch',vi.fn(async(url:string)=>url.endsWith('/execution-templates')?response([{id:'t1',templateCode:'EXEC_BRIDGE_SAFE',versionNumber:2,title:'Authorized bridge workflow',description:'High-level workflow',status:'DRAFT',effectiveFrom:'2026-08-21',effectiveUntil:null,approvedAt:null,approvedActionVersion:{id:'a3',actionCode:'ACT_INSPECT_DETAILED',versionNumber:3,title:'Detailed assessment',category:'ASSESSMENT',status:'ACTIVE'},department:null,jurisdiction:null,tasks:[{id:'tt',sequenceNumber:1,taskCode:'AUTHORIZED_INTERVENTION',title:'Authorized intervention',description:'Follow approved procedure',mandatory:true,evidenceRequired:true,verificationRequired:true,enabled:true}]}]):response([])));render(<AuthContext.Provider value={context}><PolicyActionsPage/></AuthContext.Provider>);fireEvent.click(await screen.findByRole('button',{name:'Execution templates'}));expect(screen.getByText(/EXEC_BRIDGE_SAFE · v2/)).toBeInTheDocument();expect(screen.getByText(/Approved Action ACT_INSPECT_DETAILED v3/)).toBeInTheDocument();expect(screen.getByText(/Non-authoritative draft/)).toBeInTheDocument();expect(screen.getByText(/does not approve an Action Plan or authorize execution/i)).toBeInTheDocument();expect(screen.queryByText(/AI approved|autonomous approval/i)).not.toBeInTheDocument();});});
