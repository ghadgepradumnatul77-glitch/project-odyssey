import { useState, type FormEvent } from 'react';
import { changeExecutionTaskStatus, recordExecutionEvidence, submitTaskCompletion, verifyTaskCompletion, type EvidenceType, type ExecutionTaskDto } from '../../api/workflow.api';
import { useAuth } from '../../auth/useAuth';
import { MutationFeedback } from './MutationFeedback';
export default function TaskActions({task,onRecorded}:{task:ExecutionTaskDto;onRecorded():Promise<void>|void}){const{token}=useAuth();const[mode,setMode]=useState<'block'|'cancel'|'evidence'|'complete'|'verify'|null>(null);const[submitting,setSubmitting]=useState(false);const[error,setError]=useState<unknown>(null);const[success,setSuccess]=useState<string|null>(null);async function status(status:'IN_PROGRESS'|'BLOCKED'|'CANCELLED',reason?:string){if(!token||submitting)return;setSubmitting(true);setError(null);try{await changeExecutionTaskStatus(task.id,{status,reason},token);setSuccess(status==='IN_PROGRESS'?'Recorded work marked in progress.':status==='BLOCKED'?'Blocked state recorded.':'Optional task cancellation recorded.');setMode(null);await onRecorded();}catch(value){setError(value);}finally{setSubmitting(false);}}async function form(event:FormEvent<HTMLFormElement>){event.preventDefault();if(!token||!mode||submitting)return;const data=new FormData(event.currentTarget);setSubmitting(true);setError(null);try{if(mode==='block'||mode==='cancel'){await changeExecutionTaskStatus(task.id,{status:mode==='block'?'BLOCKED':'CANCELLED',reason:String(data.get('reason')).trim()},token);}else if(mode==='complete'){await submitTaskCompletion(task.id,String(data.get('note')).trim(),token);}else if(mode==='verify'){await verifyTaskCompletion(task.id,String(data.get('note')).trim(),token);}else{const raw=String(data.get('measurementData')??'').trim();let measurementData:unknown=undefined;if(raw){try{measurementData=JSON.parse(raw);}catch{setError(new Error('invalid json'));setSubmitting(false);return;}}await recordExecutionEvidence(task.id,{evidenceType:String(data.get('evidenceType')) as EvidenceType,description:String(data.get('description')).trim(),referenceUrl:String(data.get('referenceUrl')??'').trim()||undefined,documentReference:String(data.get('documentReference')??'').trim()||undefined,measurementData,capturedAt:String(data.get('capturedAt')??'')||undefined},token);}setSuccess(mode==='complete'?'Recorded completion submitted for independent verification.':mode==='verify'?'Recorded completion independently verified.':mode==='evidence'?'Evidence reference recorded.':'Task status recorded.');setMode(null);await onRecorded();}catch(value){setError(value);}finally{setSubmitting(false);}}
 return <div className="task-actions"><p className="assignment-limitation">Assignment controls are unavailable because no scoped OFFICER candidate endpoint exists.</p><div className="action-row">{task.status==='ASSIGNED'&&<button onClick={()=>status('IN_PROGRESS')} disabled={submitting}>Mark work started</button>}{task.status==='BLOCKED'&&<button onClick={()=>status('IN_PROGRESS')} disabled={submitting}>Resume recorded work</button>}{['ASSIGNED','IN_PROGRESS'].includes(task.status)&&<button onClick={()=>setMode('block')}>Record blocked state</button>}{!task.isMandatory&&['PENDING','ASSIGNED'].includes(task.status)&&<button className="danger-action" onClick={()=>setMode('cancel')}>Cancel optional task</button>}{['IN_PROGRESS','BLOCKED'].includes(task.status)&&<button onClick={()=>setMode('evidence')}>Record evidence</button>}{task.status==='IN_PROGRESS'&&<button onClick={()=>setMode('complete')}>Submit recorded completion</button>}{task.status==='COMPLETION_SUBMITTED'&&<button onClick={()=>setMode('verify')}>Verify recorded completion</button>}</div><MutationFeedback error={error} success={success}/>{mode&&<form className="workflow-form inline-form" onSubmit={form} aria-busy={submitting} role={mode==='cancel'?'dialog':undefined} aria-modal={mode==='cancel'?true:undefined} aria-label={mode==='cancel'?'Confirm optional task cancellation':undefined}>{mode==='evidence'?<EvidenceFields/>:<><h4>{mode==='block'?'Record blocked state':mode==='cancel'?'Confirm optional task cancellation':mode==='complete'?'Submit recorded completion':'Verify recorded completion'}</h4>{mode==='verify'&&<p>Verification must be performed independently from the officer who executed/submitted the work.</p>}<label>{mode==='block'||mode==='cancel'?'Reason':'Note'}<textarea autoFocus name={mode==='block'||mode==='cancel'?'reason':'note'} required rows={3}/></label>{mode==='cancel'&&<label className="check-label"><input type="checkbox" required/> I confirm this records cancellation of this optional workflow task.</label>}</>}<div className="action-row"><button className="primary-action" disabled={submitting}>{submitting?'Recording…':'Confirm and record'}</button><button type="button" onClick={()=>setMode(null)}>Cancel</button></div></form>}</div>;
}
function EvidenceFields(){return <><h4>Record evidence reference</h4><p>This records metadata or references only. It does not upload a file or retrieve remote content.</p><label>Evidence type<select name="evidenceType" required>{['PHOTO_REFERENCE','DOCUMENT_REFERENCE','MEASUREMENT','COMPLETION_NOTE','INSPECTION_REPORT','OTHER'].map((v)=><option key={v} value={v}>{v.replaceAll('_',' ')}</option>)}</select></label><label>Description<textarea name="description" required rows={3}/></label><label>Reference URL<input name="referenceUrl" type="url"/></label><label>Document reference<input name="documentReference"/></label><label>Measurement data (valid JSON, optional)<textarea name="measurementData" rows={3}/></label><label>Captured at<input name="capturedAt" type="datetime-local"/></label></>}
