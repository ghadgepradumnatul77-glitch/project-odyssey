import{render,screen}from'@testing-library/react';import{describe,expect,it}from'vitest';import{MutationFeedback}from'./MutationFeedback';import{ApiClientError}from'../../api/errors';
describe('governed execution feedback',()=>{it('presents missing-template state without leaking server detail',()=>{render(<MutationFeedback success={null} error={new ApiClientError('secret internal query','api',409,'EXECUTION_TEMPLATE_NOT_GOVERNED')}/>);expect(screen.getByRole('alert')).toHaveTextContent(/authorized execution template has not been established for this action version/i);expect(screen.queryByText(/secret internal query/)).not.toBeInTheDocument();});});
