import{render,screen}from'@testing-library/react';import{describe,expect,it}from'vitest';import DemoGovernanceNotice,{isDemonstrationGovernanceCode}from'./DemoGovernanceNotice';
describe('demonstration governance identification',()=>{it('recognizes only explicit demo namespaces',()=>{expect(isDemonstrationGovernanceCode('DEMO-INFRA-SAFETY-001')).toBe(true);expect(isDemonstrationGovernanceCode('ACT_DEMO_INSPECT')).toBe(true);expect(isDemonstrationGovernanceCode('ACT_INSPECT_DETAILED')).toBe(false)});it('states that fixtures are not official policy',()=>{render(<DemoGovernanceNotice/>);expect(screen.getByText('DEMONSTRATION GOVERNANCE DATA')).toBeInTheDocument();expect(screen.getByText(/not official government policy/i)).toBeInTheDocument()})});
