const fs=require('node:fs');const path=require('node:path');const apiRoot=path.resolve(__dirname,'..'),buildsRoot=path.join(apiRoot,'.runtime-builds');
function resolveCurrentRuntime(){const pointer=path.join(buildsRoot,'current.json');if(!fs.existsSync(pointer))throw new Error('No packaged API runtime exists. Run npm run build first.');const{buildId}=JSON.parse(fs.readFileSync(pointer,'utf8'));if(typeof buildId!=='string'||!/^\d+-\d+$/.test(buildId))throw new Error('Packaged API runtime pointer is invalid. Run npm run build.');const server=path.join(buildsRoot,buildId,'server.js');if(!fs.existsSync(server))throw new Error('Packaged API runtime '+buildId+' is incomplete.');return{buildId,server}}
if(require.main===module)try{const runtime=resolveCurrentRuntime();console.log('Starting immutable API runtime '+runtime.buildId+'.');require(runtime.server)}catch(error){console.error(error instanceof Error?error.message:error);process.exitCode=1}
module.exports={resolveCurrentRuntime};
