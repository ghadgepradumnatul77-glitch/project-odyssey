import { describe,expect,it } from 'vitest'; import { SystemRole,UserStatus } from '../src/generated/prisma'; import { buildExecutionEvidenceReadWhere,buildExecutionPlanReadWhere,buildExecutionTaskMutationWhere,buildExecutionTaskReadWhere } from '../src/security/organizational-scope';
const a=(role:SystemRole)=>({id:'u',role,status:UserStatus.ACTIVE,departmentId:'dep',jurisdictionId:'jur'});
describe('execution scope',()=>{it.each([SystemRole.OFFICER,SystemRole.AUDITOR,SystemRole.POLICY_ADMIN])('scopes %s via Case Asset',role=>{const s={departmentId:'dep',jurisdictionId:'jur'};expect(buildExecutionPlanReadWhere(a(role))).toEqual({case:{asset:s}});expect(buildExecutionTaskReadWhere(a(role))).toEqual({executionPlan:{case:{asset:s}}});expect(buildExecutionEvidenceReadWhere(a(role))).toEqual({executionTask:{executionPlan:{case:{asset:s}}}});});it('admin read is global but mutation is scoped',()=>{expect(buildExecutionPlanReadWhere(a(SystemRole.SYSTEM_ADMIN))).toEqual({});expect(buildExecutionTaskReadWhere(a(SystemRole.SYSTEM_ADMIN))).toEqual({});expect(buildExecutionTaskMutationWhere(a(SystemRole.SYSTEM_ADMIN))).toEqual({executionPlan:{case:{asset:{departmentId:'dep',jurisdictionId:'jur'}}}});});});
