import{describe,expect,it}from'vitest';import{isLegalModelTransition,modelEvaluationSchema}from'../src/modules/predictive-models/predictive-model-governance.contracts';
const evaluation={datasetSnapshotId:'00000000-0000-4000-8000-000000000001',evaluationContractVersion:'TASK_LATENESS_EVALUATION_V1',splitStrategy:'Chronological holdout',evaluationPeriodStart:'2026-01-01',evaluationPeriodEnd:'2026-02-01',testSampleCount:10,lateCount:4,onTimeCount:6,truePositiveCount:3,trueNegativeCount:5,falsePositiveCount:1,falseNegativeCount:1,precision:.75,recall:.75,f1:.75,prAuc:.7,calibrationError:.1,falseNegativeRate:.25,baselineEvidence:[{name:'MAJORITY_CLASS',metricName:'f1',metricValue:.2,reference:'offline report'}],evaluationArtifactReference:'test://evaluation.json'};
describe('predictive model governance contracts',()=>{it('defines legal lifecycle transitions and terminal states',()=>{expect(isLegalModelTransition('EXPERIMENTAL','EVALUATED')).toBe(true);expect(isLegalModelTransition('EXPERIMENTAL','ACTIVE')).toBe(false);expect(isLegalModelTransition('EVALUATED','REJECTED')).toBe(true);expect(isLegalModelTransition('REJECTED','ACTIVE')).toBe(false);expect(isLegalModelTransition('DEPRECATED','ACTIVE')).toBe(false)});it('requires reconciled class and confusion-matrix counts',()=>{expect(modelEvaluationSchema.safeParse(evaluation).success).toBe(true);expect(modelEvaluationSchema.safeParse({...evaluation,testSampleCount:9}).success).toBe(false);expect(modelEvaluationSchema.safeParse({...evaluation,falseNegativeCount:2}).success).toBe(false)});it('validates metrics, calibration and required baseline evidence',()=>{expect(modelEvaluationSchema.safeParse({...evaluation,precision:1.1}).success).toBe(false);expect(modelEvaluationSchema.safeParse({...evaluation,calibrationError:-.1}).success).toBe(false);expect(modelEvaluationSchema.safeParse({...evaluation,baselineEvidence:[]}).success).toBe(false)});});
